# ===== AuditAgentX 镜像构建忽略 =====
# 目的：缩小构建上下文，并确保运行数据、报告、缓存、密钥、代理配置不被打进镜像。

# --- 密钥 / 环境 / 令牌 / 代理配置（绝不进镜像）---
.env
.env.*
!.env.example
*.pem
*.key
*_token
*.token
.netrc
.npmrc
proxychains.conf
proxy.conf

# --- 运行数据 / 扫描结果 / 报告 / 缓存（本地生成物，不进镜像）---
data/projects/
data/scans/
data/reports/
data/sandbox/
data/uploads/
data/*.db
data/*.sqlite
data/*.sqlite3
data/*.log
*.db
*.sqlite
*.sqlite3
reports/
*.sarif

# --- 临时 harness / 沙箱产物 / docker 日志 ---
tmp/
**/tmp/
*harness*.tmp
docker/**/*.log

# --- Python 缓存 / 虚拟环境 / 测试缓存 ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# --- 前端依赖 / 构建产物（后端镜像不需要；前端走 bind mount）---
frontend/node_modules/
frontend/dist/
frontend/tsconfig.tsbuildinfo

# --- 日志 ---
logs/
*.log

# --- Git / IDE / OS / 本地 agent 配置 ---
.git/
.github/
.gitignore
.vscode/
.idea/
.DS_Store
.claude/

# --- 文档 / 示例数据集（构建镜像非必需，可选忽略以缩小上下文）---
docs/
examples/sample_pocs/
examples/sample_reports/
